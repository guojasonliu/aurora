# Online training with SglEngine — Qwen3-Coder-Next
#
# Target model: Qwen/Qwen3-Coder-Next (80B MoE, 3B active params)
# SglEngine manages inference internally with TP=4 for the MoE model.
# Training and inference run on the same machine, no external server needed.
#
# GPU allocation (default):
#   - 4 GPUs for inference (SglEngine TP=4, target + draft model)
#   - 2 GPUs for training (DP/FSDP: draft model sharded)
#   - Total: 6+ GPUs
#
# Usage:
#   bash examples/qwen3-next-coder-online/run.sh

model:
  target_model_path: Qwen/Qwen3-Coder-Next
  trust_remote_code: true

dataset:
  train_data_path: ./datasets/onlinesd/merged/merged_train_data.jsonl
  chat_template: qwen
  prompt_key: conversations

training:
  attention_backend: flex_attention
  micro_batch_size: 4
  draft_accumulation_steps: 1
  learning_rate: 1e-4
  max_concurrent_batches: 1
  max_grad_norm: 2.5
  max_seq_length: 2048
  num_epochs: 1
  seed: 42
  train_with_decode: true
  training_num_gpus_per_node: 2
  training_num_nodes: 1
  ttt_length: 5
  warmup_ratio: 0.0
  lr_decay_style: constant

inference:
  inference_engine_type: sgl
  inference_num_gpus: 4
  inference_num_gpus_per_engine: 4
  inference_num_gpus_per_node: 8
  max_sample_pool_size: 200
  inference_buffer_threshold: 32
  inference_batch_size: 12
  sglang:
    tp_size: 4
    mem_fraction_static: 0.7

decode:
  max_new_tokens: 512
  temperature: 1.0
  speculative_algorithm: EAGLE3
  speculative_draft_model_path: null  # Auto-created from training actors
  speculative_num_steps: 5
  speculative_eagle_topk: 1
  speculative_num_draft_tokens: 6
  weight_sync_enabled: true
  weight_sync_interval: 50

mooncake:
  master_server_address: null
  metadata_server: null
  protocol: tcp
  global_segment_size: 16GB
  local_buffer_size: 4GB
  hidden_dim: 2048
  max_seq_len: 2560  # max_seq_length (2048) + max_new_tokens (512)

output_dir: ./outputs/qwen3-next-coder-online
cache_dir: ./cache
model_download_dir: null

debug:
  save_debug_train_data: null
  debug_train_only: false
  debug_inference_only: false
